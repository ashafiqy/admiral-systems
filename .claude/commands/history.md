Summarize Admiral Systems vault change history.

Read `HISTORY.md` in the admiral-systems vault root.

Present a concise summary of changes, organized by date (most recent first).

If the user provides arguments ($ARGUMENTS), use them to filter:
- A date or date range (e.g. "march", "2026-03-22", "last week") — show only matching entries
- A keyword (e.g. "linear", "slack", "client") — show only entries mentioning that topic

If no arguments are provided, show the 3 most recent dates.
